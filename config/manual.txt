{
    "writer": "CSVwriter",
    "save_path": "./this/is/a/test.csv",
    # data can be keywords or comments
    "data": "keywords",
    # cleaning can be basic, stopwords, vocabulary
    "cleaning": "[basic]"
    # model can be Fasttext, GloVe, Word2Vec, BERT, ROBERTA, ALBERT
    "model": "Fasttext",
    # experiment_mode can be "mean_of_list" or "mean_of_mean"
    "experiment_mode": "mean_of_list",
    # layers only apply to BERT (etc?) and can be 1-12, concat, pooling, etc.
    "layers": "1"

}